enum MySharedKeys { theme, language, apiToken, id, onBoarding }

enum ToastStates { success, error, warning }

enum AlertDialogType { success, error, warrning, info }

enum SimmerLoadingType {
  list,
  grid,
  card,
  text,
  circle,
  circleText,
  listOfCircles,
  listOfCard,
  listOfText,
  listOfCircleText,
  detailsScreen
}

enum ButtonType {
  facebook,
  facebookDark,
  github,
  githubDark,
  pinterest,
  apple,
  appleDark,
  twitter,
  twitterX,
  linkedin,
  google,
  googleDark,
  youtube,
  microsoft,
  tumblr,
  mail,
  reddit,
  yahoo,
  amazon,
  quora,
  instagram,
  discord,
  discordCircle,
  custom, guest
}

enum ImagePosition {
  left,
  right,
}

enum ButtonSize {
  small,
  medium,
  large,
}
